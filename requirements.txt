# Core pipeline
yfinance>=0.2.0
scipy>=1.12.0
arch>=6.0.0             # GARCH(1,1) vol scalar for position sizing
statsmodels>=0.14.0    # Required by arch
pandas>=2.0.0
numpy>=1.26.0

# Broker
alpaca-py>=0.26.0

# Scheduling
apscheduler>=3.10.0

# Market calendar (market hours detection)
pandas-market-calendars>=4.3.0

# Database
psycopg2-binary>=2.9.0

# Environment
python-dotenv>=1.0.0

# Macro data
fredapi>=0.5.0

# Sentiment analysis (FinBERT — OPTIONAL, NOT installed on Railway by default)
# PyTorch is ~2GB to install and requires ~500MB RAM at runtime.
# Railway containers (512MB-1GB) will OOM if torch is included here.
# The sentiment module degrades gracefully to HOLD signals without torch.
# To enable FinBERT locally: pip install torch transformers
vaderSentiment>=3.3.2   # Railway-safe, ~1MB
# torch>=2.0.0
# transformers>=4.40.0

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0

# 0DTE module
httpx>=0.27.0          # EventCalendar Finnhub API calls
pytz>=2024.1           # Timezone handling for ET session classifier
